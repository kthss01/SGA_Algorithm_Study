#pragma once

void Prim(struct Graph* graph, struct Vertex* startVertex, struct Graph* mst);

void Kruskal(struct Graph* graph, struct Graph* mst);

void Dijkstra(struct Graph* graph, struct Vertex* startVertex, struct Graph* mst);
